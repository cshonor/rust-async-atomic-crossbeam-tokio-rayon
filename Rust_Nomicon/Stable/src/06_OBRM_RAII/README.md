# 06 OBRM RAII — Ownership and RAII (OBRM)

**Stable** 源码目录。仅稳定 API，与 [Nightly 同章](../../Nightly/src/06_OBRM_RAII/) 对照。

```bash
cargo build --manifest-path Cargo.toml
```

（待在本目录添加 `Cargo.toml` 与示例源码）
