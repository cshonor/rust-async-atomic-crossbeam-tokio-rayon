# 11.5 Testing Channel Capacity（测试通道容量）

## 有界通道

`mpsc::channel(n)` 容量为 **n** 时，发送方在满队列上应 **阻塞** 或 `try_send` 失败。

## 测试方法

1. 填满通道且不消费；
2. 断言发送任务在 **`timeout`** 内未完成（被背压阻塞）；
3. 再 `recv` 消费一条，断言发送方可恢复。

验证系统在积压下仍能**安全恢复流转**，而非无声丢消息。

---
> 章节索引：[本章学习笔记.md](./本章学习笔记.md) · [本书详细目录](../本书详细目录.md)
