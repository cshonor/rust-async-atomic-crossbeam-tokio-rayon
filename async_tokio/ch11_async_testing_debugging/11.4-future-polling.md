## 2. 细粒度的 Future 轮询测试

对底层状态机可**手动 `poll`**（配合 **`Pin`** 与 **`noop` / 自定义 `Waker`**），断言 **`Poll::Pending` / `Poll::Ready`** 的转移顺序。

* **锁与 `Pending`**：可验证在某一 `Future` 持有异步锁时，另一竞争者在未就绪时应停在 **`Pending`**（具体 API 依 `tokio::sync::Mutex` 等而定）。

---

## 3. 示例代码：使用 mockall 模拟异步过程

书中风格接近下列意图（稳定版常用 `async-trait` 展开为 `Pin<Box<dyn Future>>` 以便 `mockall` 生成 mock）：

```rust
// 参见 tests/ch11_database_mock.rs：#[automock] + #[async_trait] + #[tokio::test]
```

---

## 总结

第 11 章为全书画上句号：异步 Rust 的测试要在**多任务、锁、网络与时序**下仍保持可预期行为，才能真正发挥「无畏并发」的工程价值。

**至此，《Async Rust》全书笔记已整理完毕。**

---

## 5. 配套代码说明（本仓库）

* **`tests/ch11_database_mock.rs`**：`mockall` + **`async-trait`** 的 `#[tokio::test]`，对应 §1.1 / §3 的 mock 思路。运行：`cargo test --test ch11_database_mock`。
* **本目录 `11.3-deadlock-probe/11.3-deadlock-probe-timeout-demo.rs`**：用 **`tokio::time::timeout`** 包裹长时间 `sleep`，演示「超时即失败」的探测方式（非真死锁，仅教学）。运行：`cargo run --example ch11_timeout_deadlock_probe`（在仓库根目录）。
* **mockito / wiremock**：按书自行加入 **`[dev-dependencies]`** 后编写 HTTP 集成测试。
* **同目录 `demo.rs`**：标准库占位。
---

> 章节索引：[本章学习笔记.md](./本章学习笔记.md) · [本书详细目录](../本书详细目录.md)
