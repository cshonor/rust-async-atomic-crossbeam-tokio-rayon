## 6. 常见陷阱与建议 (Gotchas)

* **效率差异**：Tokio 等利用 **epoll / kqueue / IOCP** 与成熟唤醒路径；自研忙轮询主要服务于**理解**，不默认等价于生产性能。
* **不要重造轮子**：生产默认仍选成熟运行时，自研栈会牺牲生态与久经沙场的边界处理。
* **调试**：可用小型 **`CountingFuture`** 等观察轮询次数与任务流向。

---

## 总结

第 10 章揭示：**互联网可以看作一个不太友好的超级 API**；掌握从零拼运行时的思路，有助于真正理解如何在 **I/O 边界**上「杂耍」多个任务而不白白占用 CPU（同时认清何时应交回给成熟框架）。

---

## 7. 配套代码说明（本仓库）

* **本目录 `10.2-std-runtime/10.2-std-runtime-tcp-nonblocking-demo.rs`**：仅 **`std`**：`TcpListener::set_nonblocking` + **`WouldBlock`** 轮询 `accept`，并用短延迟线程发起一次连接。运行：`cargo run --example ch10_std_tcp_nonblocking`（在仓库根目录）。
* **本目录 `10.2-std-runtime/10.2-std-runtime-noop-waker-demo.rs`**：仅 **`std`**：`RawWaker`/`RawWakerVTable` 构造 **no-op `Waker`** + 最小 **`block_on`** 驱动一个 `Future`（`Pending` 时 **`thread::yield_now`**，教学用）。
* **同目录 `demo.rs`**：仍为 `rustc` 占位；完整多线程执行器 + 服务器请按书逐步实现。
---

> 章节索引：[本章学习笔记.md](./本章学习笔记.md) · [本书详细目录](../本书详细目录.md)
