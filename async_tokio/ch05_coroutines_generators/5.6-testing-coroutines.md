## 6. 协程的测试优势

由于协程可以在同步代码中手动触发 `resume`，因此它们非常易于测试。

* **无需运行时**：测试纯协程逻辑往往不需要完整的 Tokio 等异步环境（与书中「可控 resume」的论点一致）。
* **确定性顺序**：你可以精确控制恢复顺序，从而更容易构造交互与边界情况（仍须注意与真实异步调度之间的差异）。

---

## 总结

第 5 章揭示了协程是异步编程的「原子」单位。掌握了协程的暂停/恢复机制，就揭开了 `await` 语法背后的面纱。正如书中所言，**互联网其实就是一个用户界面不太友好的超级 API**，而协程正是处理这些复杂流式交互的利器。

---

## 7. 配套代码说明（本仓库）

* **`5.3-simple-generator/5.3-simple-generator-state-machine-demo.rs`**：在 **stable** Rust 上用小型状态机模拟 **Yield / Resume** 与结束语义，便于对照书中「暂停—恢复」而不绑定 nightly。运行：`cargo run --example ch05_resume_state_machine`（在仓库根目录）。
* **语言内置协程**：请使用 **`cargo +nightly`** 并查阅当前 nightly 的 *Unstable Book* 中 **Coroutines / Generators** 章节启用对应 `feature` 后，将书中 `#[coroutine]` 示例粘到独立 `examples/` 或 crate 中实验。
* **同目录 `demo.rs`**：仍为 `rustc` 占位；本章以同目录配套示例与 nightly 实验为主。
---

> 章节索引：[本章学习笔记.md](./本章学习笔记.md) · [本书详细目录](../本书详细目录.md)
