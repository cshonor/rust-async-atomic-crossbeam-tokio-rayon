### 2.2 装饰器模式 (Decorator)

* **定义**：在不改变被包装对象 API 的前提下，为其附加横切能力（日志、指标、超时等）。
* **异步形态**：例如包装一个 **`Future`**，在 `poll` 前后打点；若手写 `poll`，常涉及 **`Pin`** 投影——要么 **`unsafe`** 做投影，要么用 **`Box::pin`** / 组合子库降低手写 `poll` 的频率。
---

> 章节索引：[本章学习笔记.md](./本章学习笔记.md) · [本书详细目录](../本书详细目录.md)
