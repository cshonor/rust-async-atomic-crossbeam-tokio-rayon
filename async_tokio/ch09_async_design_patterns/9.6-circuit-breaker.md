### 2.5 熔断模式 (Circuit Breaker)

* **定义**：根据失败率/连续失败次数在 **Closed / Open / Half-Open** 等状态间切换。
* **Open 时**：快速失败，避免把资源持续砸向已不健康依赖；待冷却后再探测恢复。

---

## 总结

第 9 章说明：用**隔离模块**降低接入成本，用**瀑布/装饰器/状态机**组织控制流，用**重试 + 退避**与**熔断**保护下游。把「能跑」提升为「**在生产负载下仍可预期地失败与恢复**」。

---

## 4. 配套代码说明（本仓库）

* **`9.5-retry-pattern/9.5-retry-pattern-backoff-demo.rs`**：可运行重试示例：`perform_async_call` 模拟前若干次失败，退避用**毫秒级** `1 << i`（避免书中按秒倍增在本地演示过久）；含 `#[tokio::main]`。运行：`cargo run --example ch09_retry_backoff`（在仓库根目录）。
* **隔离模块 / 熔断 / 装饰器手写 `poll`**：与项目边界强相关，建议按书在独立 crate 展开；本仓库以重试样板为主。
* **同目录 `demo.rs`**：标准库占位。
---

> 章节索引：[本章学习笔记.md](./本章学习笔记.md) · [本书详细目录](../本书详细目录.md)
