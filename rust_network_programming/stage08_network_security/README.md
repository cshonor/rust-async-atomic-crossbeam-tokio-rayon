# Stage 08 — 网络安全与生产加固

**对应书籍**：第 8 章（TLS、OpenSSL / rustls、证书等）

## 核心内容

- TLS 握手、证书链、主机名校验  
- 在 Rust 生态中选用 `native-tls` / `rustls` 等（以书为准）  
- 常见配置错误与调试思路

## 学习定位

- **生产进阶**：本地实验可晚于 Stage 03、07。  
- 现阶段：**建立概念与术语即可**，动手可等真有对外服务需求时。

## 优先级与代码

| 项目 | 建议 |
|------|------|
| 优先级 | **中（上线前拉高）** |
| 是否必写 Demo | **按需**（例如 `rustls` + `tokio-rustls` 最小 echo） |

## 学习检查

- [ ] 能说明「加密」与「身份校验（证书）」各解决什么问题  
- [ ] 知道开发环境自签名证书与生产 CA 签发的区别  

## 笔记区

（在此补充链接、摘录或 `notes.md`）
