## 7.2.3 The larger tokio ecosystem（Tokio 生态系统）

> 书 §7.2.3 · HTTP：[Ch06](../stage06_http_programming/) · TLS：[Ch08](../stage08_network_security/) · 贯通稿：[notes/Ch07-…](./notes/Ch07-使用Tokio进行异步网络编程-学习笔记.md)

---

## 一、本节在学什么

Tokio 不仅是 `TcpListener`：**周边 crate** 组成完整异步网络栈。书中部分 crate 已更名或并入主库；对照**现代常用组合**选型。

---

## 二、书中 crate → 现代对照

| 书中 crate | 作用 | 现代常见替代 |
|------------|------|--------------|
| **`tokio-io`** | 异步 Read/Write 工具 | `tokio::io`、`AsyncReadExt` / `AsyncWriteExt` |
| **`tokio-curl`** | 异步 HTTP 客户端 | **`reqwest`**（内置 Tokio，`rustls-tls`） |
| **`tokio-tls`** | 非阻塞 TLS | **`tokio-rustls`**、`native-tls` + tokio |
| **`tokio-core`** | Reactor 核心 | **`tokio::runtime`**、`#[tokio::main]` |

---

## 三、常一起使用的生态

| crate | 作用 | 关联章 |
|-------|------|--------|
| **hyper** | HTTP 客户端/服务端底层 | Ch6 |
| **axum / actix-web** | Web 框架（基于 hyper/Tokio） | Ch6 延伸 |
| **reqwest** | 高级 HTTP 客户端 | Ch6、Ch8 HTTPS |
| **tonic** | gRPC | Ch5 |
| **tower** | Service trait、中间件、超时/限流 | Ch6/7 |
| **tokio-util** | Codec、`Framed`、兼容层 | 7.2.2 |
| **bytes** | 零拷贝 buffer | Codec、hyper |

```text
典型 HTTPS 客户端栈：
  reqwest → hyper → tokio-rustls → tokio::net::TcpStream

典型 Web 服务端栈：
  axum → hyper → tokio-rustls → tokio::spawn accept loop
```

---

## 四、本书 Ch7 ↔ 仓库 `async_tokio/`

| 本书主题 | 建议对照目录 |
|----------|--------------|
| Future / async 入门 | `async_tokio/ch01_async_intro/` |
| Future 组合、并发 | `ch02`～`ch03` |
| Channel / 共享状态 | `ch06_async_channels` |
| Tokio 运行时、任务 | `ch07_tokio_*` |
| 优雅关闭 | `ch07_tokio_graceful_shutdown/` |
| 测试与竞态 | `ch11_async_testing_debugging/` |

根 workspace `Cargo.toml` 中 `ch07_*`、`ch01_reqwest_join` 等 **[[example]]** 可逐条运行。

---

## 五、新手易错

1. **crate 版本/feature 对齐**：`reqwest` 默认 TLS 与 `tokio` feature 需与项目一致（见 Ch8 `rustls-tls`）。
2. **在 Tokio 外 poll async I/O**：hyper/reqwest 假设在 Tokio runtime 内；CLI 用 `#[tokio::main]` 或 `Runtime::block_on`。
3. **重复造轮子**：分帧优先 `tokio_util::codec`；HTTP 优先 hyper/axum，不必手写 HTTP 解析。
4. **忽视 `tower` 抽象**：限流、超时、日志可叠在 Service 层，与业务 handler 分离。

---

## 本节 Demo 一览

| Bin | 说明 |
|-----|------|
| `demo_7_2_3_ecosystem` | 打印现代 Tokio 生态速查表 |

```bash
cargo run --manifest-path rust_network_programming/stage07_tokio_async_net/Cargo.toml --bin demo_7_2_3_ecosystem
```

TLS 深入：[stage08_network_security](../stage08_network_security/README.md) · 章索引：[本章学习笔记.md](./本章学习笔记.md)
