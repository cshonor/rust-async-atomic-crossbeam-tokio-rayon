## 7.2 Heading to tokio（走向 Tokio 框架）

> 书 §7.2

---

**Tokio** = Reactor（`mio`）+ task 调度 + `tokio::net`。`accept().await` 不阻塞其它连接。

```text
Codec → Protocol → Service（书中分层；现代常 hyper/axum 封装 HTTP）
```

### 配套代码

| Bin | 说明 |
|-----|------|
| `demo_7_2_echo_server` | `127.0.0.1:37082` |
| `demo_7_2_echo_client` | 异步回显客户端 |
