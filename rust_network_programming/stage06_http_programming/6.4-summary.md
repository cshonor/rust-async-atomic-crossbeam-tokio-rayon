## 6.4 Summary（本章总结）

> 书 §6.4 · 贯通稿：[notes/Ch06-在互联网上进行HTTP通信-学习笔记.md](./notes/Ch06-在互联网上进行HTTP通信-学习笔记.md) · 章索引：[本章学习笔记.md](./本章学习笔记.md)

---

## 一、一句话回顾

用 Rust 生态三件套处理 HTTP：**Hyper**（底层异步服务底座）、**Rocket**（宏路由全栈 Web）、**reqwest**（高层客户端）；数据格式靠 **Serde**（Ch4），运行时从 Ch3 阻塞 **演进到 Tokio 异步**（Ch6/7）。

---

## 二、核心术语

| 术语 | 含义 |
|------|------|
| **HTTP/1.1** | 文本头 + 长度/chunked 体；keep-alive |
| **HTTP/2** | 多路复用；gRPC 默认传输（Ch5） |
| **Hyper** | HTTP 解析/生成 + Tokio 驱动连接 |
| **`SO_REUSEPORT`** | 多监听同端口，内核分摊新连接 |
| **Rocket 路由宏** | `#[get]` / `#[post]` 绑定 handler |
| **Data Guard** | 请求体/参数提取与校验（常配合 Serde） |
| **diesel + r2d2** | ORM + 连接池（书中博客 CRUD） |
| **reqwest** | 阻塞/异步 HTTP 客户端 |
| **`blocking::Client`** | 同步 reqwest，适合 CLI |

---

## 三、各节对照

| 书 § | 主题 | 笔记 | 关键概念 / API |
|------|------|------|----------------|
| 6.1 | Hyper | [6.1](./6.1-introducing-hyper.md) | 报文形态、单 reactor、REUSEPORT |
| 6.2 | Rocket | [6.2](./6.2-introducing-rocket.md) | `#[launch]`、`Json<T>`、diesel/Tera |
| 6.3 | reqwest | [6.3](./6.3-introducing-reqwest.md) | `Client::get().send()`、`.json()` |
| 6.4 | Summary | 本节 | 三库分工、自检、下一步 |

---

## 四、三库分工

| Crate | 角色 | 典型用途 |
|-------|------|----------|
| **Hyper** | HTTP 协议 + 异步 I/O 底座 | 自建服务器、网关、底层代理 |
| **Rocket** | 全栈 Web 框架 | 博客 API、模板页、快速 REST |
| **reqwest** | HTTP 客户端 | 调第三方 API、爬虫、微服务间 HTTP |

```text
写服务：Hyper（底层） 或 Rocket（高层）
写客户端：reqwest
数据格式：Serde JSON（Ch4）
运行时：Tokio（Ch3 阻塞 → Ch6/7 异步）
```

### 与 Ch3 阻塞模型对照

| | **Ch3 `std::net`** | **Ch6 Hyper/Rocket** |
|---|-------------------|----------------------|
| I/O | 阻塞 | 非阻塞 + async |
| 并发 | 每连接一线程 | reactor / 异步任务 |
| 协议 | 自己组帧或裸字节 | HTTP 库解析 |
| 学习价值 | Socket 与 TCP 语义 | 应用层 Web 编程 |

---

## 五、本章自检

- [ ] 能说明 Hyper 偏底层、Rocket 偏全栈、reqwest 偏客户端  
- [ ] 能描述最小 HTTP 响应：状态行、头、空行、Body  
- [ ] 理解单 reactor 与 `SO_REUSEPORT` 多监听提升吞吐的思路  
- [ ] 知道 Rocket Data Guard 与 Serde 在入参校验上的关系  
- [ ] 会用 `reqwest` 发 GET（`demo_6_3`）并说明 POST `.json()` 链  
- [ ] 知道 6.2 Rocket 为何使用独立 `Cargo.toml`  

---

## 六、学习建议

- 不做 Web 网关时：**先掌握 §6.3 reqwest + Serde**，Hyper/Rocket 可与 stage07 后移。  
- 与 **stage07** 一起学：Tokio 搞懂后，Hyper/reqwest 异步代码才顺手。  
- **§6.1 raw TCP demo** 建议亲手 `curl` 一次，再读 Hyper 文档中的 server 示例。

---

## 七、下一步

| 方向 | 路径 |
|------|------|
| Tokio 异步网络 | [stage07](../stage07_tokio_async_net/) |
| HTTPS / TLS | [stage08](../stage08_network_security/) |
| gRPC on HTTP/2 | [Ch05](../stage05_app_protocol/) |
| TCP/Socket 基础 | [Ch03](../stage03_std_tcp_udp/) |
| Serde | [Ch04](../stage04_serde_parse/) |

---

## 八、新手易错（全章）

1. **手写 HTTP 报文漏 `\r\n`** → 用库或对照 §6.1 demo 格式。  
2. **混淆「服务」与「客户端」crate** → 本章服务看 6.1/6.2，调 API 看 6.3。  
3. **Rocket 与章根 Cargo 混编** → 6.2 单独 manifest，命令带 `--manifest-path`。  
4. **阻塞 server 线程模型照搬 Ch3** → Hyper 是异步多连接，不是一线程一连接。  
5. **外网 reqwest demo 失败** → 先查网络，再查 TLS feature。

---

## 本节 Demo 一览

| Bin | 端口 / 说明 |
|-----|-------------|
| `demo_6_1_raw_http` | `127.0.0.1:18080`，`std::net` 最小 HTTP |
| `demo_6_2_rocket_hello` | `127.0.0.1:18082`，独立 crate |
| `demo_6_3_reqwest_get` | 外网 `httpbin.org` |
| `demo_6_4_summary` | 打印上述入口 |

```bash
cargo run --manifest-path rust_network_programming/stage06_http_programming/Cargo.toml --bin demo_6_4_summary

cargo run --manifest-path rust_network_programming/stage06_http_programming/Cargo.toml --bin demo_6_1_raw_http
cargo run --manifest-path rust_network_programming/stage06_http_programming/Cargo.toml --bin demo_6_3_reqwest_get
cargo run --manifest-path rust_network_programming/stage06_http_programming/6.2-introducing-rocket/Cargo.toml --bin demo_6_2_rocket_hello
```

文件：[6.4-summary-demo.rs](./6.4-summary/6.4-summary-demo.rs)

章索引：[本章学习笔记.md](./本章学习笔记.md)
