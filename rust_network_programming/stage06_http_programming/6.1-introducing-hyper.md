## 6.1 Introducing Hyper（Hyper 框架简介）

> 书 §6.1 · 全章贯通稿：[notes/Ch06-在互联网上进行HTTP通信-学习笔记.md](./notes/Ch06-在互联网上进行HTTP通信-学习笔记.md)

---

### HTTP 在栈中的位置（复习）

```text
应用：HTTP 语义（方法、头、状态码、Body）
  ↓
传输：TCP（常见）或 TLS（HTTPS）
  ↓
… Ch1 分层、Ch3 Socket …
```

- **HTTP/1.1**：文本头 + 长度或 chunked 体；一问一答为主（可 keep-alive）。  
- **HTTP/2**：多路复用、头部压缩；**gRPC 默认跑在 H2 上**（见 [Ch05](../stage05_app_protocol/)）。  
- 本章以 **HTTP/1.1 风格的服务/客户端 API** 为主；现代 `hyper` 已支持 H1/H2。

### Hyper 简介

**Hyper** 是 Rust 生态里成熟、底层的 **HTTP 实现/库**（偏框架基石，而非「自带路由的全家桶」）。

| 特点 | 说明 |
|------|------|
| 角色 | 解析/生成 HTTP、驱动连接；常与 **Tower** 中间件组合 |
| 并发模型 | 服务端基于 **Tokio + async/await** |
| 适用 | 高并发、需精细控制连接与协议的场景 |

**现代对照**：书内可能仍出现 `tokio-core`、`net2` 等旧名；新项目用 **`tokio` 1.x + `hyper` 1.x**。

### 单线程与多核扩展

- 单 reactor：模型简单，单核吞吐受限。  
- **`SO_REUSEPORT`** + 多 Tokio 运行时 / 多监听：连接分摊到多 reactor，吞吐可近翻倍（书中示例）。

```text
         ┌─ thread 1 ─ Hyper + Tokio
:8080 ───┼─ thread 2 ─ Hyper + Tokio   （REUSEPORT）
         └─ thread N ─ …
```

**延伸**：[Ch07](../stage07_tokio_async_net/) · `async_tokio/`。

### 配套代码

- [6.1-introducing-hyper-raw-tcp-demo.rs](./6.1-introducing-hyper/6.1-introducing-hyper-raw-tcp-demo.rs) — 用 `std::net` 手写最小 HTTP/1.0 响应，理解 Hyper 之下的报文形态（再读 Hyper 官方 server 示例）。
