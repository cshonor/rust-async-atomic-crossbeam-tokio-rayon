# Stage 04 — 序列化与协议解析

**对应书籍**：第 4 章（serde、文本 / 二进制协议）

## 核心内容

- `serde` / `serde_json`（或书中等价栈）  
- 自定义二进制布局、字节序、变长字段  
- 与网络读写边界的配合（粘包、半包）

## 学习定位

- **工程必备**：后续 RPC、自有协议、量化数据流都依赖这一层。  
- 可与 Stage 03 的小协议 Demo 合并练习。

## 优先级与代码

| 项目 | 建议 |
|------|------|
| 优先级 | **高** |
| 是否必写 Demo | **是**：至少「JSON 消息体」+「一种紧凑二进制布局」 |

## 建议 Demo 清单

- [ ] 结构体 `Serialize` / `Deserialize`，经 TCP 发送与还原  
- [ ] 手动 `Read` 解析一种固定头 + 负载的格式  
- [ ] （可选）`nom` / 组合子解析（与 Stage 09 联动）  

## 笔记区

（在此补充链接、摘录或 `notes.md`）
