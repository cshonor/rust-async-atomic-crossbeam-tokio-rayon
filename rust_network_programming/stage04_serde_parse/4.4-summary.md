## 4.4 Summary（总结）

> 书 §4.4 · 贯通稿：[notes/Ch04-…](./notes/Ch04-数据序列化反序列化与解析-学习笔记.md) · 章索引：[本章学习笔记.md](./本章学习笔记.md) · 上一节：[4.3 二进制解析](./4.3-parsing-binary.md)

---

## 一、全章在解决什么问题

[Ch3](../stage03_std_tcp_udp/) 传的是**字节流**；本章把字节变成应用能用的**结构**：

```text
应用结构体  ←→  Serde（JSON/二进制）  ←→  Vec<u8>
文本行协议  ←→  nom PEG（tag、take_until）  ←→  字段切片
定长/位域帧  ←→  nom（be_u32、take、bits!）  ←→  强类型头 + payload
```

**分帧**（长度前缀、换行、HTTP Content-Length）在 I/O 层；**Serde / nom** 只管「已切好的一段」如何编码或解码。

---

## 二、按节回顾

| 书 § | 笔记 | 核心技能 |
|------|------|----------|
| 4.1 | [4.1-serde-serialization.md](./4.1-serde-serialization.md) | `#[derive(Serialize, Deserialize)]` + `serde_json` |
| 4.1.1 | [4.1.1-custom-serde.md](./4.1.1-custom-serde.md) | `#[serde(rename)]`、手写 `Visitor`、`serde_test` |
| 4.2 | [4.2-parsing-textual.md](./4.2-parsing-textual.md) | `nom` 解析 HTTP 请求行等文本 |
| 4.3 | [4.3-parsing-binary.md](./4.3-parsing-binary.md) | 长度前缀、`be_u32`、位域与 IPv6 概念 |
| 4.4 | 本节 | 选型与自检 |

---

## 三、技术选型速查

| 场景 | 推荐工具 | 理由 |
|------|----------|------|
| REST / 调试友好消息体 | Serde + `serde_json` | 人机可读、跨语言 |
| Rust 服务间紧凑消息 | Serde + `bincode` / postcard | 体积小、derive 即用 |
| wire 字段名 ≠ Rust 字段 | `#[serde(rename)]` 或手写 Serialize | 见 [4.1.1](./4.1.1-custom-serde.md) |
| HTTP 请求行、SMTP 文本命令 | `nom` 文本组合子 | 比硬套 JSON 自然 |
| 标准二进制头、位域 | `nom` `be_u32`、`bits!` | 精确对齐协议文档 |
| TCP 多条 JSON | 长度前缀 + Serde body | [4.1](./4.1-serde-serialization.md) + [4.3](./4.3-parsing-binary.md) |
| 完整 HTTP 服务 | hyper 等 | [stage06](../stage06_http_programming/) |

---

## 四、与相邻章节的衔接

| 方向 | 路径 | 关系 |
|------|------|------|
| 阻塞 Socket | [stage03 / Ch3](../stage03_std_tcp_udp/) | `TcpStream` + 本章消息体与分帧 |
| 应用层 RPC/FTP | [stage05](../stage05_app_protocol/) | 协议语义在本章解析之上 |
| HTTP 编程 | [stage06](../stage06_http_programming/) | 请求行理解 → hyper 实战 |
| 解析理论 | [stage09](../stage09_appendix_deep/) | PEG、pest 等深化 |

建议练习：**Ch3 echo/TCP demo + 本章长度前缀 JSON**，把粘包问题在真实 `read` 循环里走通一遍。

---

## 五、本章自检

- [ ] 会用 `#[derive(Serialize, Deserialize)]` + `serde_json::to_vec` / `from_slice`
- [ ] 能说明 TCP 流上为何需要**长度前缀**或**分隔符**（Serde 不解决粘包）
- [ ] 知道何时用 `#[serde(rename)]`，何时才手写 `Serialize` / `Visitor`
- [ ] 能解释 `nom` 里 `tag`、`take_until1`、`tuple` 的作用
- [ ] 能区分文本 PEG（HTTP 行）与二进制 `be_u32` + `take`（长度前缀帧）
- [ ] 知道 `complete` 解析器假设输入已完整，流式 TCP 要缓冲或 `streaming`

---

## 六、新手易错（全章汇总）

1. **只学 Serde 不做分帧**：`from_slice` 在粘包环境必踩坑。
2. **一种工具包打天下**：JSON 体 + 二进制头很常见，应组合 [4.1](./4.1-serde-serialization.md) 与 [4.3](./4.3-parsing-binary.md)。
3. **nom 与 Serde 角色混淆**：nom 切**语法**；Serde 映射**已识别的一段**到类型。
4. **忽略字节序与 `len` 校验**：二进制协议安全第一。
5. **过早引入完整 HTTP 库**：先 [4.2](./4.2-parsing-textual.md) 理解请求行，再上 stage06。

---

## 本节 Demo 一览

| Bin | 说明 |
|-----|------|
| `demo_4_4_summary` | 打印本章全部 demo 入口名 |
| `demo_4_1_serde_point3d` | §4.1 Point3D JSON 往返 |
| `demo_4_1_1_custom_rename` | §4.1.1 `serde(rename)` |
| `demo_4_2_nom_http_line` | §4.2 HTTP 请求行 |
| `demo_4_3_binary_length_prefix` | §4.3 长度前缀帧 |

```bash
cargo run --manifest-path rust_network_programming/stage04_serde_parse/Cargo.toml --bin demo_4_4_summary
cargo run --manifest-path rust_network_programming/stage04_serde_parse/Cargo.toml --bin demo_4_1_serde_point3d
cargo run --manifest-path rust_network_programming/stage04_serde_parse/Cargo.toml --bin demo_4_1_1_custom_rename
cargo run --manifest-path rust_network_programming/stage04_serde_parse/Cargo.toml --bin demo_4_2_nom_http_line
cargo run --manifest-path rust_network_programming/stage04_serde_parse/Cargo.toml --bin demo_4_3_binary_length_prefix
```

章索引：[本章学习笔记.md](./本章学习笔记.md) · 贯通稿：[notes/Ch04-…](./notes/Ch04-数据序列化反序列化与解析-学习笔记.md)
