## 4.3 Parsing binary data（解析二进制数据）

> 书 §4.3

---

协议头常按**位/定长字段**布局；`nom` 的 **`bits!` / `take!`** 切分 `&[u8]`。与 **Serde+bincode** 对比：标准头/位域 → nom；双方 Rust 结构体 → Serde。

### 配套代码

- [4.3-parsing-binary-length-prefix-demo.rs](./4.3-parsing-binary/4.3-parsing-binary-length-prefix-demo.rs)
