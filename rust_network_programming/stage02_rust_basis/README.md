# Stage 02 — Rust 语言与生态前置

**对应书籍**：第 2 章（Rust 生态与语言要点）

## 核心内容

- 所有权、借用、`trait`  
- 错误处理（`Result` / `?`）  
- 与网络相关的：迭代器、并发原语初印象

## 学习定位

- **快速翻阅**：你已有并发与异步基础时，只补「网络代码里常踩的坑」。  
- 不必整章精读。

## 优先级与代码

| 项目 | 建议 |
|------|------|
| 优先级 | **低**（查漏补缺） |
| 是否必写 Demo | **否**（需要时写 10～20 行验证即可） |

## 学习检查

- [ ] `Result` 在 I/O 边界上的传播习惯  
- [ ] `Send` / `'static` 与跨线程任务的关系（与 Tokio `spawn` 对照 `async_tokio/`）  

## 笔记区

（在此补充链接、摘录或 `notes.md`）
