# Chapter 10 — 检测、调试与 LLVM 生态工具

| 项目 | 说明 |
|------|------|
| **学习策略** | **精读** |
| **对应书** | 《Learn LLVM 17》第 10 章 |

## 本目录用途

- **Sanitizer / 竞态检测 / 性能分析** 等与书一致的工具链使用笔记（以你本机已安装栈为准）。  
- 与仓库：对照 `atomic/` 多线程例、`async_tokio` 并发场景的排错记录。

## 目录约定

| 子目录 | 说明 |
|--------|------|
| `notes/` | 命令模板、输出解读、与 rustc 文档链接 |
| `code/` | 可选：最小可复现 crate 路径说明（非必须） |

## IR / 日志归档

工具输出可摘要后放入 `notes/`，大文件勿入库；必要时仅存**脱敏**片段到 `ir_samples/optimize_compare/` 并注明工具名与版本。
