## 1.9 Summary（本章小结）

> 书 §1.9 · 章索引：[本章学习笔记.md](../本章学习笔记.md) · [全书目录](../全书目录-与实体书一致.md)

---

## 一、一句话回顾

Rust 并发从**线程共享内存**出发，用**共享借 / 排他借**在编译期防数据竞争；跨线程用 **`'static` / `scope` / `Arc`** 解决所有权；可变共享靠 **内部可变性 + `Mutex`/`RwLock`/原子**；线程边界由 **`Send`/`Sync`** 标记；等条件用 **park / `Condvar`**，且必须 **`loop` 查条件** 防虚假唤醒。

---

## 二、核心术语

| 术语 | 含义 |
|------|------|
| **Shared / Exclusive Reference** | `&T` / `&mut T` 的准确叫法 |
| **Data Race** | 并发读写同内存 → UB |
| **JoinHandle\<T\>** | `spawn` 返回值；详见 [1.1.2 JoinHandle](../1.1-threads-in-rust/1.1.2-join-handle.md) |
| **Interior Mutability** | 通过 `&T` 改内部数据 |
| **Send / Sync** | 跨线程移所有权 / 跨线程共享 `&T` |
| **Lock Poisoning** | 持锁 panic 后锁标记中毒 |
| **Spurious Wake-up** | `park` / `wait` 可无因苏醒 → **必须 `loop`** |
| **Leakpocalypse** | 依赖 Drop 的 scoped API 因 leak 而 Unsound |
| **Writer Starvation** | `RwLock` 读者占满，写者长期等待 |

---

## 三、各节对照

| 书 § | 主题 | 笔记 | 关键词 |
|------|------|------|--------|
| 1.1 | 线程 | [1.1 索引](../1.1-threads-in-rust/1.1-threads-in-rust.md) · … [1.1.3 Send/'static](../1.1-threads-in-rust/1.1.3-send-static-spawn.md) | `spawn`、`join`、`move`、`Arc` |
| 1.2 | 作用域线程 | [1.2](../1.2-scoped-threads/1.2-scoped-threads.md) | `scope`、`'scope` vs `'static` |
| 1.3 | 共享所有权 | [1.3](../1.3-shared-ownership/1.3-shared-ownership.md) | `Rc` / `Arc`、克隆遮蔽 |
| 1.4 | 借用与数据竞争 | [1.4](../1.4-borrowing-data-races/1.4-borrowing-data-races.md) | 共享读 OR 独占写 |
| 1.5 | 内部可变性 | [1.5 索引](../1.5-interior-mutability/1.5-interior-mutability.md) · [1.5.3 Cell](../1.5-interior-mutability/1.5.3-cell.md) / [1.5.4 RefCell](../1.5-interior-mutability/1.5.4-refcell.md) | `Cell` / `RefCell` |
| 1.6 | Send / Sync | [1.6 索引](../1.6-send-sync/1.6-send-sync.md) · [1.6.5 `Arc<Mutex>`](../1.6-send-sync/1.6.5-arc-mutex-combined.md) | `Arc<Mutex<T>>` |
| 1.7 | 互斥锁 | [1.7 索引](../1.7-mutex-rwlock/1.7-mutex-rwlock.md) · [1.7.3 guard 陷阱](../1.7-mutex-rwlock/1.7.3-mutex-guard.md) | `MutexGuard`、中毒 |
| 1.8 | 等待 | [1.8 索引](../1.8-parking-condvar/1.8-parking-condvar.md) · [1.8.3 Condvar](../1.8-parking-condvar/1.8.3-condvar-pattern.md) | `park`、`Condvar::wait` |

---

## 四、Demo 与运行逻辑

| 序号 | 主题 | 文件 | 逻辑要点 |
|------|------|------|----------|
| 1 | `spawn` + `join` | [1.1-threads-in-rust/](../1.1-threads-in-rust/) | `join` 等待；无 `join` 时 `main` 退出可杀子线程 |
| 2 | `thread::scope` | [1.2-scoped-threads-demo.rs](../1.2-scoped-threads/code/1.2-scoped-threads-demo.rs) | 借局部变量；scope 结束自动 join |
| 3 | `Arc` + 遮蔽 | [1.3-shared-ownership-arc-demo.rs](../1.3-shared-ownership/code/1.3-shared-ownership-arc-demo.rs) | `let a = a.clone(); move \|\| …` |
| 4 | `Mutex` 累加 | [1.7-mutex-rwlock-mutex-demo.rs](../1.7-mutex-rwlock/code/1.7-mutex-rwlock-mutex-demo.rs) | **锁内不要 `sleep`** |
| 5 | Parking | [1.7-mutex-rwlock-thread-advanced-demo.rs](../1.7-mutex-rwlock/code/1.7-mutex-rwlock-thread-advanced-demo.rs) | 空队列 `park`，生产 `unpark` |
| 6 | `Condvar` | [1.8-parking-condvar-demo.rs](../1.8-parking-condvar/code/1.8-parking-condvar-demo.rs) | `while` + `wait` + `notify` |

```bash
cargo build --manifest-path atomic/Cargo.toml
```

库入口：`study_atomic::run_ex1_default()`、`run_extended()`（见 `atomic/src/mod.rs`）。

---

## 五、本章自检

- [ ] 能写出 `spawn` + `join`，说明 `JoinHandle<T>` 与 `Result<T, _>`
- [ ] 能解释 `scope` 与 `'static` spawn 的差别
- [ ] 知道 `Rc` vs `Arc`，`RefCell` vs `Mutex`
- [ ] 能背出 **Send / Sync** 各问什么
- [ ] 会写 `Arc<Mutex<T>>` 多线程累加
- [ ] 知道 **MutexGuard 作用域陷阱** 与 **Condvar 虚假唤醒**

---

## 六、新手易错（全章）

| # | 坑 | 正确做法 |
|---|-----|----------|
| 1 | 锁持有过久 | `{}` / `drop(guard)` 后再 I/O |
| 2 | `if let` 里 `lock()` | 先 pop 再分支 |
| 3 | `if` 等 Condvar | 改 **`while` / `loop`** |
| 4 | `Rc`/`RefCell` 跨线程 | 换 **`Arc` + `Mutex`** |
| 5 | 信 Drop 一定执行 | 见 **Leakpocalypse** → 用 `thread::scope` |
| 6 | 无 `join` 以为子线程必跑完 | `main` 退出强杀；或 sleep 演示 |

---

## 七、面试 / 背诵

**一段话**：Rust 并发用借用规则防数据竞争；跨线程用 `'static`/`Arc`/`scope`；可变共享用 `Mutex`/`Condvar`/原子；`Send`/`Sync` 划边界。

| # | 极简句 |
|---|--------|
| 1 | 同进程线程共享内存；`&T` 共享借、`&mut T` 排他借。 |
| 2 | `spawn` → `JoinHandle`；`.join()` 阻塞取 `Result<T, _>`。 |
| 3 | `scope` 可借局部；`Arc` 长期共享；`Mutex` 包数据。 |
| 4 | `park`/`Condvar` 等条件必 `loop`；早放锁。 |

---

## 八、下一步

| 方向 | 路径 |
|------|------|
| 原子与内存序 | [Chapter 02](../Chapter-02-Atomics/) |
| 网络多线程 | [rust_network_programming Ch3](../../rust_network_programming/stage03_std_tcp_udp/) |
| Tokio 对照 | [1.1.6 §四](../1.1-threads-in-rust/1.1.6-thread-builder-tokio.md) |
