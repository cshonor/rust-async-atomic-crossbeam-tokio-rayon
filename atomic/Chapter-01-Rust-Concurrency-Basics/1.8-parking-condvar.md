## 1.8 Waiting: Parking and Condition Variables（Park 与条件变量）



- `thread::park()` 休眠；`Thread::unpark()` 唤醒。  
- **`unpark` 先于 `park`**：信号**不丢失**，下次 `park` **直接返回**。  
- **局限**：适合简单一对一通知；多生产者/多消费者下精准唤醒特定线程很复杂。

### `Condvar`

- 必须与 **`Mutex`** 绑定；`condvar.wait(guard)` **原子地**解锁并休眠，唤醒后**重新加锁** — 解决「放锁与休眠之间丢通知」的竞态。  
- `notify_one()` / `notify_all()`。  
- **生产者-消费者、惊群、持锁 notify**：见 [Condvar与条件变量-贯通笔记.md](../Condvar与条件变量-贯通笔记.md)；代码 [1.8-parking-condvar-demo.rs](./1.8-parking-condvar/1.8-parking-condvar-demo.rs)  
- **虚假唤醒**：`park` / `wait` 可能无信号自行苏醒 → 条件检查必须在 **`while`/`loop`** 内，**不能**单独 `if`。

---
> **Demo**：[1.8-parking-condvar/](./1.8-parking-condvar/) · 章索引：[本章学习笔记.md](./本章学习笔记.md)
