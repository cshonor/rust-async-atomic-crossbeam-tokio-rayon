## 1.1.6 `where F: FnOnce() -> T` 语法拆解

> 书 §1.1（续）· FnOnce **语义** vs move：[1.1.5](./1.1.5-closure-traits-fnonce.md) · `spawn` 签名：[1.1.4](./1.1.4-send-static-spawn.md)

---

## 标准写法

```rust
fn demo<T, F>() where F: FnOnce() -> T {}

// 与尖括号内约束完全等价：
fn demo<T, F: FnOnce() -> T>() {}
```

**`where`**：后置泛型约束；约束多、行长时**可读性更好**。

---

## 一、逐个拆解

### 1. `F` — 泛型参数

`F` 代表**可调用对象**（闭包、函数指针、自定义 `impl FnOnce` 的类型）。名字随意：`Func`、`Closure` 均可。

### 2. `where F: Trait` — 约束

限定：**类型 `F` 必须实现**后面这个 trait（及关联类型绑定）。

### 3. `FnOnce() -> T` — 三大 Callable trait 之一

Rust 函数/闭包分 **`Fn` / `FnMut` / `FnOnce`**（子类型关系见 [1.1.5](./1.1.5-closure-traits-fnonce.md)）。

#### ① `FnOnce`

- **Trait 规则**：`call_once(self, …)` — 调用**消耗 F 自身**，故**最多一次**。
- **常见场景**：`move` 捕获 **owned** 变量时，编译器往往**只**实现 `FnOnce`（≠ trait 定义写了 move，见 1.1.5）。

```rust
let s = String::from("hello");
let closure = move || s;
closure();
// closure(); // 错误：F 已被消耗 / 捕获资源已 move 出
```

#### ② `()` — 参数列表

`()` = **无参**闭包。  
双参数示例：`FnOnce(i32, &str) -> T`。

#### ③ `-> T` — 返回值

闭包执行结果类型为 **`T`**（`T` 为外层泛型）。与 `spawn` 的 **`JoinHandle<T>`** 中 `T` 同一角色。

---

## 二、一句话翻译

```rust
where F: FnOnce() -> T
```

> **`F` 必须是无参、返回 `T`、且按 `FnOnce` 规则最多调用一次的可调用对象。**

`thread::spawn` 里在此基础上还要 **`F: Send + 'static`**（[1.1.4](./1.1.4-send-static-spawn.md)）。

---

## 三、三 trait 对比（调用 + 捕获习惯）

| Trait | 调用时 `self` | 典型调用次数 | 常见捕获方式（习惯，非 trait 字面） |
|-------|---------------|--------------|--------------------------------------|
| **`Fn`** | `&self` | 多次 | 共享引用 `&T` |
| **`FnMut`** | `&mut self` | 多次 | 可变引用 `&mut T` |
| **`FnOnce`** | **`self`（消耗）** | **一次** | 常伴随 **move owned**；也可无捕获（见 1.1.5） |

**兼容（自动 impl）**：

- `Fn` ⇒ 也有 `FnMut` + `FnOnce`
- `FnMut` ⇒ 也有 `FnOnce`
- **反过来不成立**

---

## 四、实战：`run_once`

```rust
fn run_once<T, F>(f: F) -> T
where
    F: FnOnce() -> T,
{
    f() // 消耗 F
}

fn main() {
    let msg = String::from("test");
    let c = move || msg;
    let res = run_once(c);
    println!("{res}");
    // c(); // 错误：c 已 move 进 run_once
}
```

与 **`jh.join()`** 类似：闭包/句柄**只消费一次**。

---

## 五、语法糖：`-> T` 与 `Output = T`

`FnOnce() -> T` 是糖。Trait 真实形状（简化）：

```rust
trait FnOnce<Args> {
    type Output;
    fn call_once(self, args: Args) -> Self::Output;
}
```

| 写法 | 展开 |
|------|------|
| `FnOnce() -> T` | `FnOnce<(), Output = T>` |
| `where F: FnOnce() -> T` | `where F: FnOnce<(), Output = T>` |

**`Output = T`**：把关联类型 **`Output`** 绑定为 **`T`** — 即你看到的「等于」语法，**不是** `F = F` 这种无意义相等。

高阶约束偶尔写成：

```rust
where
    F: FnOnce(),
    F::Output = T,
// 与 F: FnOnce() -> T 等价
```

---

## 六、与 `spawn` 签名对照

```rust
pub fn spawn<F, T>(f: F) -> JoinHandle<T>
where
    F: FnOnce() -> T + Send + 'static,
    T: Send + 'static,
```

| 片段 | 含义 |
|------|------|
| `F: FnOnce() -> T` | 无参闭包，返回 `T`，执行一次 |
| `F: Send + 'static` | 闭包可跨线程 move，不借短生命周期 |
| `T: Send + 'static` | 返回值可经 `join` 移回 |

---

## 七、总结（背诵）

1. **`where`**：后置约束，`F` 必须实现指定 trait。  
2. **`FnOnce() -> T`**：`()` 无参；`-> T` = **`Output = T`**。  
3. **`FnOnce`**：`call_once(self)` 消耗自身 → 最多调用一次（见 1.1.5 与 move 区别）。  
4. **整句**：`F` 是无参、返回 `T`、FnOnce 可调用对象。

---

§1.1 索引：[1.1-threads-in-rust.md](./1.1-threads-in-rust.md)
