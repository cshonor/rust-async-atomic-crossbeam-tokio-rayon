# Chapter 01 — Rust 并发基础

## 本章目标



## 要点梳理



## 与本章代码的对应

- **crate 模块源码**：与本 `note.md` **同一目录**下的多个 `*.rs`（`mod.rs` 为模块根），由 **`src/mod.rs`** 以 `#[path = "../Chapter-01-Rust-Concurrency-Basics/mod.rs"]` 引入为 `study_atomic::chapter_01`。
- 单文件运行：各示例文件内常有 `pub fn main()`，可在本目录对某个文件执行 `rustc xxx.rs`（视依赖而定；与 workspace 联编请用 `cargo build -p study_atomic`）。
