# 5.1 One-Shot Channel

> Demo：[5.1/](./5.1/) — `5.1-one-shot-channel-demo.rs` · `study_atomic::chapter_05::demo()`

## 要点

- `MaybeUninit` + `Release/Acquire` + `park/unpark`。  
- `send`/`receive` 消耗 `self`，一次收发。

## 扩展

- [ ] 与 `mpsc` 对比
