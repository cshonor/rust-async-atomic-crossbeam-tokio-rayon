# 互斥锁与锁体系 — 跨章贯通笔记

> **书目澄清**：《Rust Atomics and Locks》**第 6 章是「构建我们自己的 Arc」**（原子引用计数），**不是** Mutex 专章。  
> **Mutex / 锁的原理、使用与优化** 分布在：**第 1 章**（基础）、**第 4 章**（自旋锁）、**第 8 章**（OS 原语）、**第 9 章**（自建锁）。  
> 本章笔记按**主题**串联；代码见各章 `*.rs`。

---

## 全书章节与「锁」的对应

| 章 | 文件夹 | 与锁的关系 |
|----|--------|------------|
| 1 | `Chapter-01-Rust-Concurrency-Basics` | `Mutex`/`RwLock`/`Condvar` 入门 |
| 4 | `Chapter-04-Spin-Locks` | 用户态自旋锁（`spin_lock.rs`） |
| 6 | `Chapter-06-Custom-Arc` | **Arc 引用计数**（非 Mutex） |
| 8 | `Chapter-08-OS-Primitives` | futex、`WaitOnAddress` 等 |
| 9 | `Chapter-09-Custom-Locks` | [9.1 三态 Mutex](./Chapter-09-Custom-Locks/9.1-mutex.md)、[9.3 写优先 RwLock](./Chapter-09-Custom-Locks/9.3.2-writer-starvation-state-machine.md) |

---

## 1. Mutex 底层原理与 Rust 的特殊设计

### 1.1 数据与锁绑定（相对 C/C++）

| C/C++ 常见形态 | Rust `Mutex<T>` |
|----------------|-----------------|
| 锁与数据分离，易忘记加锁 | **锁包裹数据**；必须先 `lock()` 得 `MutexGuard` 才能访问 |
| 裸指针绕过 | 类型系统强制「有锁才有 `&mut T`」 |

### 1.2 状态与阻塞

- 概念上：**未锁定 / 已锁定**。  
- 争用失败 → 线程**阻塞**，内核调度走 CPU（非忙等）。

### 1.3 底层 OS 支持（第 8 章展开）

| 平台 | 典型原语 |
|------|----------|
| Linux | **futex** |
| macOS | `os_unfair_lock` 等 |
| Windows | SRWLock、`WaitOnAddress` 等 |

高效阻塞依赖这些**用户态快速路径 + 内核等待**的组合。

---

## 2. 锁守卫（Guard）与作用域

### 2.1 RAII：无 `.unlock()`

- `lock()` → **`MutexGuard`**（实现 **`Deref` / `DerefMut`**，用起来像 `&mut T`）。  
- **`Drop` 时自动解锁** — 避免忘记释放。

### 2.2 经典陷阱：`if let` 延长锁（第 1 章必考）

**错误**（Guard 活到整个 `if let` 结束）：

```rust
if let Some(item) = list.lock().unwrap().pop() {
    process(item); // 仍持锁！
}
```

**正确**：

```rust
let item = list.lock().unwrap().pop();
if let Some(item) = item {
    process(item); // 锁已释放
}
```

→ 代码：`use_mutex.rs`、`use_mutex_guard_lifetime.rs`

---

## 3. 三种锁的选型

| 类型 | 行为 | 适用 | 注意 |
|------|------|------|------|
| **`Mutex<T>`** | 独占；阻塞休眠 | **绝大多数**共享可变状态 | 尽快 `drop(guard)` |
| **`RwLock<T>`** | 多读 **或** 一写 | **读多写少** | 详见 **[RwLock与读写锁体系-贯通笔记.md](./RwLock与读写锁体系-贯通笔记.md)**（写者饥饿、第 9 章写优先） |
| **自旋锁**（第 4 章） | 忙等 + `spin_loop` | 临界区**极短**、多核 | 持锁久 → 烧 CPU；见 `spin_lock.rs` |

**对比**：自旋 **无 syscall**；`Mutex` **有**休眠/唤醒成本。第 5 章 Channel 用 **park** 等消息，不用自旋等锁。

---

## 4. 异常、死锁与处理

### 4.1 锁中毒（Lock Poisoning）

- 持锁线程 **panic** → `Mutex` 标记 **poisoned**。  
- 后续 `lock()` 得 **`Err`** — 防止读到半写入的不一致状态。  
- 处理：`lock().unwrap_or_else(|e| e.into_inner())` 等（按业务决定是否恢复）。

### 4.2 死锁

**借用检查器不能防止死锁。**

| 类型 | 说明 |
|------|------|
| **递归加锁** | 同线程再次 `lock` 同一 `Mutex`；POSIX 可能 UB/永久阻塞；Windows CRITICAL_SECTION 可递归但在 Rust 会产生多个 `&mut T` → **UB**；安全实现**禁止**递归 |
| **规避** | 锁粒度最小；多把锁时**全局固定加锁顺序** |

---

## 5. 性能损耗与优化（第 9 章思路）

### 5.1 损耗来源

1. **缓存行争用**（多核抢同一 cache line）  
2. **内核 syscall**（休眠/唤醒昂贵）

### 5.2 优化 1：三态，减少无谓 syscall

| 状态 | 含义 |
|------|------|
| 0 | 未锁定 |
| 1 | 已锁定，**无等待者** |
| 2 | 已锁定，**有等待者** |

- **解锁**：仅当状态为 **2** 时才 `wake_one`。  
- **加锁失败**：仅当把 1→2 成功后才 `wait`。  
- **无竞争**路径：**零 syscall**。

### 5.3 优化 2：混合锁（Hybrid）

- 真正 `wait` 前先 **自旋** 若干次（如 100× `spin_loop`）。  
- 锁在窗口内释放 → 直接抢到，省调度。

### 5.4 优化 3：假共享（False Sharing）

- CPU 按 **缓存行（常 64B）** 缓存。  
- 多个无关锁挤在同一行 → 一核改锁使它核行失效。  
- **高频多锁**：`#[repr(align(64))]` 分散到不同行。  
- **锁与保护数据**：紧凑相邻有时更利于一次拉入缓存（视访问模式而定）。

---

## 6. 与各章代码对照

| 主题 | 文件 |
|------|------|
| Mutex / 中毒 / if let 陷阱 | `Chapter-01/.../use_mutex.rs`、`use_mutex_guard_lifetime.rs` |
| Condvar + Mutex | `use_condvar.rs` |
| RwLock 概念 | 第 1 章笔记；标准库 `std::sync::RwLock` |
| 自旋锁 | `Chapter-04/spin_lock.rs`（`lock` / `lock_cas`） |
| Arc（第 6 章，非 Mutex） | 待补 `Chapter-06` 实现 |
| OS 原语 | `Chapter-08` 笔记占位 |
| 工业锁优化 | [Chapter-09/9.1-mutex.md](./Chapter-09-Custom-Locks/9.1-mutex.md) |

---

## 7. 面试一句话

**Rust 的 `Mutex<T>` 用类型系统把数据和锁绑在一起，`MutexGuard` RAII 自动解锁；争用走内核阻塞；读多写少用 `RwLock`；极短临界区用自旋；panic 中毒、禁止递归死锁；高性能锁靠三态减 syscall、混合自旋、缓存行对齐防假共享。**

---

## 8. 极简背诵卡

| # | 背这句 |
|---|--------|
| 0 | 第 6 章是 Arc 不是 Mutex；锁分散在 1/4/8/9 章。 |
| 1 | Mutex 包数据；lock 得 Guard，Drop 解锁，无 unlock。 |
| 2 | if let 里 lock 会拖长 Guard；先 let 再 if let。 |
| 3 | Mutex 阻塞；RwLock 多读一写，防写者饥饿与 SRW 死锁。 |
| 4 | 自旋：极短临界区+多核；长持锁用 Mutex。 |
| 5 | 中毒=持锁 panic；lock 得 Err 防半写数据。 |
| 6 | 借用检查不防死锁；多锁固定顺序，禁止递归 lock。 |
| 7 | 优化：三态减 wake/wait；混合自旋；align(64) 防假共享。 |
