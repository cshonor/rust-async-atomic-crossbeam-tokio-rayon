## 8.3.3 Rust `std` 与 OS 原语映射

> 书 §8.3 · 索引：[8.3-condition-variables.md](./8.3-condition-variables.md) · 前：[8.3.2 Windows](./8.3.2-windows-condvar.md) · 后：[8.4 总结](./8.4-summary.md)

---

## 总表

| Rust API | 典型底层（概念） |
|----------|------------------|
| `Mutex::lock` 阻塞 | futex WAIT / pthread mutex / critical section |
| `MutexGuard` drop | 用户态解锁 + futex WAKE |
| `Condvar::wait(guard)` | 释放 mutex + cond 上 WAIT（或 futex REQUEUE 路径） |
| `Condvar::notify_one` | signal / WakeConditionVariable / WakeByAddressSingle |
| `Condvar::notify_all` | broadcast / WakeAll / WakeByAddressAll |
| `thread::park` / `unpark` | 平台线程调度 + 可能 futex（实现细节） |

日常 **safe Rust 不直接 syscall** —— std 封装上述原语。

---

## 章节分工（再强调）

| 层 | 章节 |
|----|------|
| **怎么用** `Condvar` + `while` | [第 1 章 §1.8](../Chapter-01-Rust-Concurrency-Basics/1.8-parking-condvar/1.8-parking-condvar.md) |
| **OS 是什么** | **本章** |
| **手写 wait/wake、减 syscall** | [第 9 章](../Chapter-09-Custom-Locks/本章学习笔记.md) |
| **贯通** | [Condvar与条件变量-贯通笔记.md](../Condvar与条件变量-贯通笔记.md) |

---

## 与第 9 章衔接

书中 **`atomic_wait::wait` / `wake_one` / `wake_all`**：

- Linux → **futex**；
- Windows → **WaitOnAddress**；
- 供自建 Mutex / Condvar / RwLock 使用。

---

§8.3 索引：[8.3-condition-variables.md](./8.3-condition-variables.md)
