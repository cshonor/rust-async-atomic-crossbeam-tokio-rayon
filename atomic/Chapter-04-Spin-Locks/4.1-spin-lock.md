# 4.1 自旋锁与 Acquire / Release

> Demo：[4.1/](./4.1/) — `4.1-spin-lock-demo.rs`

## 要点

- `swap(Acquire)` 加锁、`store(false, Release)` 解锁。  
- 对照 `2.6` Release/Acquire 与 `Mutex` 阻塞。

## 扩展

- [ ] `lock_cas` 与 `lock()` 对照
