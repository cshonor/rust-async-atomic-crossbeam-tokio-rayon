# Chapter 02 — 原子操作

## 本章目标



## 要点梳理



## 与本章代码的对应

- **crate 模块源码**：与本 `note.md` **同一目录**下的多个 `*.rs`（`mod.rs` 为模块根），由 **`src/mod.rs`** 以 `#[path = "../Chapter-02-Atomics/mod.rs"]` 引入为 `study_atomic::chapter_02`。
- 单文件运行：各示例内常有 `pub fn main()`；与 workspace 联编请用 `cargo build -p study_atomic`。
