# 《Rust Atomics and Locks》完整目录（与实体书一致）

**作者：Mara Bos** · 共 **10 章 + 附录**（英文原版 / 中文版结构相同）

> 仓库学习区：`atomic/Chapter-01` … `Chapter-10` · 每章 **`本章学习笔记.md`** 为完整精读主文档  
> 小节 **`X.Y-english-slug.md`** 仅为索引（链到本章学习笔记 + demo）  
> 与旧 slug 对照：[章节与小节对照表.md](./章节与小节对照表.md)

---

## Chapter 1 – Basics of Rust Concurrency（Rust 并发基础）

1. **Threads in Rust**（Rust 中的线程）
2. **Scoped Threads**（作用域线程）
3. **Shared Ownership and Reference Counting**（共享所有权与引用计数）
   - Statics（静态变量）
   - Leaking（内存泄漏）
   - Reference Counting（Rc / Arc）
4. **Borrowing and Data Races**（借用与数据竞争）
5. **Interior Mutability**（内部可变性）
   - Cell
   - RefCell
   - Mutex and RwLock
   - Atomics
   - UnsafeCell
6. **Thread Safety: Send and Sync**（线程安全：Send 与 Sync）
7. **Locking: Mutexes and RwLocks**（加锁：互斥锁与读写锁）
   - Rust's Mutex（Rust 的互斥锁）
   - Lock Poisoning（锁中毒）
   - Reader-Writer Lock（读写锁）
8. **Waiting: Parking and Condition Variables**（等待：线程停靠与条件变量）
   - Thread Parking（park / unpark）
   - Condition Variables（条件变量）
9. **Summary**（本章小结）

---

## Chapter 2 – Atomics（原子操作）

1. **Atomic Load and Store Operations**（原子加载与存储）
   - Example: Stop Flag（示例：停止标志）
   - Example: Progress Reporting（示例：进度报告）
   - Synchronization（同步）
   - Example: Lazy Initialization（示例：惰性初始化）
2. **Fetch-and-Modify Operations**（获取-修改操作）
   - Example: Progress Reporting from Multiple Threads（多线程进度报告）
   - Example: Statistics（统计）
   - Example: ID Allocation（ID 分配）
3. **Compare-and-Exchange Operations**（比较-交换 / CAS）
   - Example: ID Allocation Without Overflow（无溢出 ID 分配）
   - Example: Lazy One-Time Initialization（一次性惰性初始化）
4. **Summary**（本章小结）

---

## Chapter 3 – Memory Ordering（内存排序）

1. **Reordering and Optimizations**（重排与优化）
   - Compiler Reordering（编译器重排）
   - Hardware Reordering（硬件重排）
2. **The Memory Model**（内存模型）
   - Happens-Before Relationship（先行发生关系）
   - Spawning and Joining（线程创建与合并）
3. **Memory Order Options**（内存序选项）
   - Relaxed（松散序）
   - Release and Acquire（释放/获取序）
   - Example: Locking（示例：锁）
   - Example: Lazy Initialization with Indirection（带间接寻址的惰性初始化）
   - Consume（消费序）
   - Sequentially Consistent（顺序一致性）
4. **Fences**（内存屏障）
5. **Common Misconceptions**（常见误区）
6. **Summary**（本章小结）

---

## Chapter 4 – Building Our Own Spin Lock（构建自旋锁）

1. **A Minimal Implementation**（最简实现）
2. **An Unsafe Spin Lock**（不安全自旋锁）
3. **A Safe Interface Using a Lock Guard**（带守卫的安全接口）
4. **Summary**（本章小结）

---

## Chapter 5 – Building Our Own Channels（构建通道）

1. **A Simple Mutex-Based Channel**（基于 Mutex 的简单通道）
2. **An Unsafe One-Shot Channel**（不安全一次性通道）
3. **Safety Through Runtime Checks**（运行时检查保证安全）
4. **Safety Through Types**（通过类型保证安全）
5. **Borrowing to Avoid Allocation**（借用避免分配）
6. **Blocking**（阻塞）
7. **Summary**（本章小结）

---

## Chapter 6 – Building Our Own "Arc"（构建 Arc）

1. **Basic Reference Counting**（基础引用计数）
2. **Testing It**（测试）
3. **Mutation**（可变性）
4. **Weak Pointers**（弱指针）
5. **Testing It**（弱指针测试）
6. **Optimizing**（优化）
7. **Summary**（本章小结）

---

## Chapter 7 – Understanding the Processor（理解处理器）

1. **Processor Instructions**（处理器指令）
   - Load and Store（加载/存储）
   - Read-Modify-Write（读-改-写）
   - x86 lock prefix（x86 锁前缀）
   - x86 compare-and-exchange（x86 CAS 指令）
   - Load-Linked and Store-Conditional（LL/SC）
   - ARM load-exclusive / store-exclusive（ARM 独占指令）
   - Compare-and-exchange on ARM（ARM CAS）
2. **Caching**（缓存）
   - Cache Coherence（缓存一致性）
   - Write-Through Protocol（写直达协议）
   - MESI Protocol（MESI 协议）
   - Impact on Performance（性能影响）
3. **Reordering**（重排）
   - Memory Ordering（内存序）
   - x86-64: Strongly Ordered（强序）
   - ARM64: Weakly Ordered（弱序）
   - An Experiment（实验）
4. **Memory Fences**（内存屏障）
5. **Summary**（本章小结）

---

## Chapter 8 – Operating System Primitives（操作系统原语）

1. **Futex**（快速用户空间互斥量）
   - Waiting（等待）
   - Waking（唤醒）
   - Shared Futexes（共享 futex）
2. **Thread Parks**（线程停靠）
3. **Condition Variables**（条件变量）
4. **Summary**（本章小结）

---

## Chapter 9 – Building Our Own Locks（构建各类锁）

1. **Mutex**（互斥锁）
   - Avoiding System Calls（避免系统调用）
   - Optimizing Further（进一步优化）
   - Benchmarks（基准测试）
2. **Condition Variable**（条件变量）
   - Avoiding System Calls（避免系统调用）
   - Avoiding Spurious Wakeup（避免虚假唤醒）
3. **Reader-Writer Lock**（读写锁）
   - Avoiding Writer Busy Loop（避免写者忙循环）
   - Avoiding Writer Starvation（避免写者饥饿）
4. **Summary**（本章小结）

---

## Chapter 10 – Projects（项目与灵感）

1. **Semaphores**（信号量）
2. **RCU**（读-复制-更新）
3. **Lock-Free Linked List**（无锁链表）
4. **Queue-Based Locks**（基于队列的锁）
5. **Blocking Locks**（阻塞锁）
6. **Seqlocks**（顺序锁）
7. **Teaching Materials**（教学资料）

---

## 附录（Appendix）

- **Appendix A** – The Rust Memory Model（Rust 内存模型）
- **Appendix B** – Bibliography（参考文献）

见 [Appendix/](./Appendix/)
